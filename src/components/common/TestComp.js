import React, {PropTypes} from 'react';

const TestComp = () => {
    return (
        <div>
            <p>So this could be a common comp to show the current page.</p>
        </div>
    );
};

export default TestComp;